// Geolocation boundary.
//
// Feature code reaches geocoding/location helpers through this module so the
// underlying provider can be swapped without changing feature code.
export * from '../../utils/geocode'
